import { Controller, Get, Post, Query, Param, Body, HttpCode, HttpStatus, NotFoundException, UseGuards, SetMetadata } from '@nestjs/common';

@Controller('market')
export class MarketController {

  constructor() {}

}
